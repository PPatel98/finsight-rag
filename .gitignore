# Virtual environment — don't upload packages
venv/

# ChromaDB database — don't upload local data
chroma_db/

# API keys and environment files — never upload these
.env
.zprofile

# Python cache files — not needed
__pycache__/
*.pyc
*.pyo

# Mac system files
.DS_Store

# Data folder — PDFs are too large for GitHub
data/
